# Week 5 — Stuck Points

- [Q 5.1] Forgot the norm-squared = v^T v identity needed to introduce R^T R — revisit: ||v||² as inner product, link to orthogonal-matrix property R^T R = I.
- [Q 5.1] Forgot the cos θ formula linking dot product to angle — revisit: u · w = ||u|| ||w|| cos θ, solved for cos θ as inner-product over norm product.
- [Q 5.1] Inverted the cos θ ratio (wrote norms/dot instead of dot/norms) — revisit: divide both sides of u · w = ||u|| ||w|| cos θ by ||u|| ||w||; cos θ has the inner product in the numerator.
- [Q 5.1] Confused why (Ru)·(Rw) = (Ru)^T(Rw) — revisit: column-vector dot product a·b is shorthand for a^T b; same identity used to rewrite ||v||² = v^T v in part 1.
- [Q 5.2] Assumed Jacobian is always square — revisit: for f: R^n → R^m, J is m×n (rows = outputs, cols = inputs); entry (i,j) = ∂f_i/∂x_j.
- [Q 5.2] Flipped Jacobian rows/cols (wrote n×m instead of m×n) — revisit: rows = outputs, cols = inputs; mnemonic: J maps R^n to R^m, so J · v has m components.
- [Q 5.2] Didn't see how to verify full rank of a non-square Jacobian — revisit: pick a k×k sub-matrix where k = min(m,n); nonzero determinant of any such sub-matrix proves rank ≥ k; here the bottom 2×2 of J is the identity, so rank = 2 everywhere.
- [Q 5.2] Conflated the map f with the latent space — revisit: f: R^n → R^m is a function; R^n (the domain) is the latent space; R^m is the ambient space; the image of f is the manifold. Jacobian is evaluated at a point in the domain.
- [Q 5.2] Couldn't construct f^{-1} for the embedded-manifold proof — revisit: read off the inverse from f's output components; f₂ = x₁ and f₃ = x₂ ⟹ f^{-1}(y₁, y₂, y₃) = (y₂, y₃), the projection onto the last two coordinates; projection is continuous → f is a homeomorphism onto its image → embedding.
- [Q 5.2] Confused the *name* of the inverse map with the conclusion (called it "embedded map") — revisit: the map (y₁,y₂,y₃) ↦ (y₂,y₃) is a coordinate projection π; projections are linear hence continuous; the conclusion (embedding) follows from continuous f + continuous f⁻¹ + injective.
- [Concept] Inverse function — revisit: f⁻¹(y) = x iff f(x) = y; requires f injective; to *find* f⁻¹, set f(x)=y and solve for x in terms of y; round-trip f⁻¹(f(x)) = x; for f(x₁,x₂) = (2x₁²+x₂², x₁, x₂), inverse is f⁻¹(y₁,y₂,y₃) = (y₂,y₃) — first coord of input is redundant.
- [Q 5.3] Skipped computing ||ċ(t)|| and used the hint's antiderivative as if its integrand √(1+t²) were the speed — revisit: arc length = ∫ ||ċ(t)|| dt; always compute the norm first (here ||ċ(t)|| = 2√(1+t²), factor of 2 was missed) before matching any antiderivative hint.
- [Concept] Pullback metric M(x) = J_f(x)^T J_f(x) — revisit: latent tangent v maps to ambient tangent J·v; ||J·v||² = v^T M v gives the *true* manifold inner product expressed in latent coords; M is n×n symmetric (n = latent dim); entries M_ij = (col i of J) · (col j of J); this is the matrix that appears in LMLG Eq. 5.21's angle formula.
- [Concept] Positive definite (symmetric M) — three equivalent defs: (1) v^T M v > 0 for all nonzero v; (2) all eigenvalues > 0; (3) all leading principal minors > 0 (Sylvester). For diagonal M, eigenvalues = diagonal entries — so diag(a,b) is pos def iff a>0 AND b>0.
- [Concept] Diagonal matrix — square matrix with all off-diagonal entries = 0; eigenvalues are exactly the diagonal entries (no characteristic-polynomial computation needed).
- [Q 5.4] Initially didn't see *why* pos def matters — revisit: a metric needs v^T M v > 0 for nonzero v so every direction has strictly positive length; zero eigenvalue → degenerate parametrization (sphere poles are the canonical example: x₁ ∈ {0, π} ⟹ sin²x₁ = 0 ⟹ M singular ⟹ longitude meaningless); negative eigenvalue → not Riemannian. Domain (0, π) excludes poles for exactly this reason.
